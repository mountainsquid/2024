Session #: v007
Date: Feb/10/2024
Time studied: 20 minutes 

01. What is a hash?
02. A unique length of string attach to a data. 
03. Hash got generated by putting the data through a hash function 
04. The length of the hash doesn't change 
05. Blockchain = block + hash + nounce + data 
06. nounce = number used once for proof of work that creates the blockchain 
07. Blockchain ... a chain of blocks that shows proof of work :) 
08. Genesis block = the first block in the blockchain 
09. Each block has a hash to show "proof of work" and immutable 
